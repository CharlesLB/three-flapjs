import React from 'react';

import { Container, NewSectionButton } from './styles';

const SessionContainerNewSession: React.FC = () => {
  return (
    <Container>
      <NewSectionButton>Create new Session</NewSectionButton>
    </Container>
  );
};

export default SessionContainerNewSession;
